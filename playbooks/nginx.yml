---
- name: install nginx
  hosts: fea_nginx
  become: true

  tasks:
  - name: add epel-release repo
    yum:
      name: epel-release
      state: present

  - name: install nginx
    yum:
      name: nginx
      state: present

  - name: start nginx
    service:
      name: nginx
      state: started