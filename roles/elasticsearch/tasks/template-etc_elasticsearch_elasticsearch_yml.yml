---
- name: (template) scp etc/elasticsearch/logging.yml.j2 /etc/elasticsearch/logging.yml
  template:
    src: etc/elasticsearch/logging.yml.j2
    dest: /etc/elasticsearch/logging.yml
    owner: elasticsearch
    group: elasticsearch
    mode: 0776
